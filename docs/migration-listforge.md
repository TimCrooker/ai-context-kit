# ListForge Migration (Planned Later)

1. Add `@timothycrooker/ai-context-cli`.
2. Keep existing generated outputs committed.
3. Replace local build script call with `ai-context build`.
4. Replace local verify script call with `ai-context verify`.
5. Compare generated outputs and adjust scope mappings until parity.
6. Remove old generator scripts once outputs are stable.
